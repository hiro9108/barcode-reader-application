import { CameraPreviewOptions } from "@capacitor-community/camera-preview";

export const cameraPreviewOptions: CameraPreviewOptions = {
  parent: "content",
  position: "front",
  disableAudio: true,
};
